# 🚀 Roadmap: GUI Библиотека с нуля

## 📋 TL;DR - С чего начать прямо сейчас

**Первый день (2-3 часа):**

1. Создать структуру директорий
2. Написать базовые примитивы (Point, Size, Rect, Color)
3. Создать простой рендерер на wgpu
4. Отобразить окно с цветом фона

**Первая неделя:**

- MVP: Окно + прямоугольник + текст
- Понять wgpu и event loop
- Создать базовый Widget trait

## 🎯 Пошаговый план

### Шаг 0: Подготовка (Сделано ✅)

- [x] Установлены зависимости (winit, wgpu, tokio)
- [x] Создан базовый Cargo проект
- [x] Есть event loop (хотя и пустой)

### Шаг 1: Структура проекта (Следующий)

```bash
src/
├── core/
│   ├── mod.rs
│   ├── geometry.rs
│   └── color.rs
├── render/
│   ├── mod.rs
│   └── renderer.rs
├── app/
│   ├── mod.rs
│   └── app.rs
├── lib.rs
└── main.rs
```

### Шаг 2: Базовые примитивы

**Файл: `src/core/geometry.rs`**

```rust
// Точки, размеры, прямоугольники, отступы
```

**Файл: `src/core/color.rs`**

```rust
// Цвета, RGBA, конвертация
```

**Файл: `src/core/mod.rs`**

```rust
pub mod geometry;
pub mod color;

pub use geometry::*;
pub use color::*;
```

### Шаг 3: Рендерер на wgpu

**Файл: `src/render/renderer.rs`**

```rust
pub struct Renderer {
    // wgpu поля
}

impl Renderer {
    pub async fn new(window: Window) -> Self { /* ... */ }
    pub fn clear(&mut self, view: &wgpu::TextureView, color: Color) { /* ... */ }
    pub fn draw_rect(&mut self, view: &wgpu::TextureView, rect: Rect, color: Color) { /* ... */ }
}
```

**Файл: `src/render/mod.rs`**

```rust
pub mod renderer;
pub use renderer::*;
```

### Шаг 4: App и интеграция

**Файл: `src/app/app.rs`**

```rust
pub struct App {
    event_loop: EventLoop<()>,
}

impl App {
    pub fn new() -> Self { /* ... */ }
    pub fn run(self) -> ! { /* ... */ }
}
```

**Файл: `src/app/mod.rs`**

```rust
pub mod app;
pub use app::*;
```

### Шаг 5: Обновление lib и main

**Файл: `src/lib.rs`**

```rust
pub mod core;
pub mod render;
pub mod app;

pub mod prelude {
    pub use crate::core::*;
    pub use crate::app::App;
}
```

**Файл: `src/main.rs`**

```rust
use brul_gui::prelude::*;

fn main() {
    println!("🚀 Brul GUI starting...");
    let app = App::new();
    app.run();
}
```

### Шаг 6: Cargo.toml

```toml
[package]
name = "brul-gui"
version = "0.1.0"
edition = "2024"

[dependencies]
winit = "0.30.12"
wgpu = "27.0.1"
tokio = { version = "1.49.0", features = ["full"] }
pollster = "0.3"  # Для блокирующего async
```

## 🎯 Что должно заработать после Шага 6

```bash
cargo run
```

Должно открыться окно 800x600 с белым фоном (или любым другим цветом).

## 📝 Проверочные вопросы перед началом

1. **Какой цвет фона для теста?** (suggest: Color::WHITE)
2. **Какой размер окна?** (suggest: 800x600)
3. **Нужен ли заголовок окна?** (suggest: "Brul GUI")
4. **Как обрабатывать ошибки?** (suggest: panic! для MVP)

## 🔧 Отладка и тестирование

### Что проверять на каждом шаге:

- [ ] Компилируется ли?
- [ ] Открывается ли окно?
- [ ] Есть ли ошибки в консоли?
- [ ] Можно ли закрыть окно?

### Как тестировать:

```bash
# Компиляция
cargo check

# Запуск
cargo run

# Форматирование
cargo fmt

# Linting
cargo clippy
```

## 📚 Ресурсы для изучения

### wgpu:

- [wgpu官网](https://wgpu.rs/)
- [wgpu examples](https://github.com/gfx-rs/wgpu/tree/master/wgpu/examples)
- [Learn wgpu](https://sotrh.github.io/learn-wgpu/)

### winit:

- [winit docs](https://docs.rs/winit)
- [winit examples](https://github.com/rust-windowing/winit/tree/master/examples)

### GUI concepts:

- [Druid architecture](https://github.com/linebender/druid)
- [Iced architecture](https://github.com/iced-rs/iced)
- [React as a mental model](https://react.dev/)

## ⏱️ Временные оценки (без учета изучения)

- Шаг 1-2 (структура + примитивы): 30-60 минут
- Шаг 3 (рендерер): 1-2 часа
- Шаг 4-5 (app + интеграция): 30-60 минут
- Шаг 6 (отладка): 30 минут

**Итого MVP**: 3-5 часов чистого кодинга

## 🎓 Что вы узнаете

### Технические навыки:

- wgpu API
- Event loop в Rust
- 2D геометрия
- Базовая архитектура GUI

### Архитектурные решения:

- Модульная структура
- Trait-based design
- Error handling
- API design

## 🚦 Критерии успеха

### MVP успех:

- [ ] Окно открывается
- [ ] Фон отрисовывается
- [ ] Можно закрыть окно
- [ ] Нет ошибок в консоли

### Следующий уровень:

- [ ] Рисуется прямоугольник
- [ ] Работает текст
- [ ] Есть интерактивность
- [ ] Поддержка layout

## 🤔 Что делать, если застряли?

### Проблема: wgpu не компилируется

**Решение**: Проверить версии, попробовать `cargo update`

### Проблема: Окно не открывается

**Решение**: Проверить event loop, добавить логирование

### Проблема: Черный экран

**Решение**: Проверить clear color, view creation

## 📖 Следующие документы для чтения

1. `gui_architecture.md` - полная архитектура
2. `implementation_details.md` - детали кода
3. `decisions.md` - архитектурные решения

## ✅ Чек-лист перед стартом

- [ ] Понимаете ли вы план?
- [ ] Есть ли вопросы по архитектуре?
- [ ] Готовы ли начать с MVP?
- [ ] Понимаете ли вы wgpu basics?

---

**Готовы начать?** Выберите:

- **A**: Начать с создания структуры проекта
- **B**: Сначала изучить wgpu подробнее
- **C**: Задать уточняющие вопросы
